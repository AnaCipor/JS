// let number = 20000;
let number = 2;
while (number < 2050){
    console.log(number);
    number *= 2;
    let myName = 'Fedya'
    let res = number%2 === 0 ? "even":"odd";
    console.log(res, myName);
}
// console.log(res); // ERROR



// while (условие) { 
//     действие;
// }
// ** Пока условие true, повторяй действие.

/*

let n = 1;        // 1. СТАРТ

while (n <= 5) {  // 2. УСЛОВИЕ
    console.log(n);
    n++;          // 3. ИЗМЕНЕНИЕ
}

*/
